[project]
name = "seo-link-building-api"
version = "0.1.0"
description = "Backend API for the SEO Link Building AI Platform (Sprint 1: Backend + Database)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
